services:
  db:
    image: postgres:15-alpine
    container_name: ez-db
    environment:
      POSTGRES_USER: ezapply
      POSTGRES_PASSWORD: ezapply_dev_pw
      POSTGRES_DB: ezapply_dev
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  pgdata:
