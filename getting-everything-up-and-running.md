Getting everything running. Make sure docker desktop is running

1. Get the database running using 
docker-compose up -d

2.  Starting Backend Server
cd server; pnpm run dev

3. Starting Frontend Electron App
cd app; pnpm start

4. Starting Vite Dev Server for Browser Testing
cd app; pnpm run dev
